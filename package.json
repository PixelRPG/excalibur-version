{
  "name": "pixelrpg",
  "private": true,
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "yarn workspaces foreach -v run test",
    "build": "yarn build:excalibur && yarn workspaces foreach -ptv --no-private --from '{tile-extruder,@pixelrpg/esbuild,@excaliburjs/plugin-tiled,@excaliburjs/plugin-aseprite}' run build && yarn build build:example",
    "clear": "yarn workspaces foreach -v run clear",
    "start": "yarn workspace @pixelrpg/tuxemon run start",
    "build:excalibur": "yarn workspace excalibur run build && yarn workspace excalibur run build:esm",
    "build:example": "yarn workspace @pixelrpg/tuxemon run build"
  },
  "author": "Pascal Garber <pascal@artandcode.studio>",
  "license": "GPL-3.0-or-later",
  "workspaces": [
    "examples/*",
    "packages/*",
    "packages/tile-extruder/workspaces/*"
  ],
  "devDependencies": {
    "@pixelrpg/eslint-config": "file:packages/eslint-config",
    "typescript": "^5.1.6"
  },
  "packageManager": "yarn@3.6.1"
}
