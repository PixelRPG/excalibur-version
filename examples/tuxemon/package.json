{
  "name": "@pixelrpg/tuxemon",
  "version": "0.0.0",
  "description": "",
  "main": "src/main.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run template && esbuilder --outfile ./dist/main.js",
    "template": "mkdir -p ./dist && cp ./src/index.html ./dist/index.html",
    "serve:dev": "npm run serve -- --dev",
    "serve": "npm run template && esbuilder --serve --outfile ./dist/main.js",
    "start:dev": "yarn run serve:dev",
    "start": "npm run serve",
    "clear": "rimraf dist"
  },
  "author": "Pascal Garber <pascal@artandcode.studio>",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "excalibur": "^0.26.0-alpha.262"
  },
  "devDependencies": {
    "esbuild": "^0.13.9",
    "@pixelrpg/esbuild": "^0.0.1"
  }
}
